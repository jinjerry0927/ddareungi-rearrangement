# 프로젝트 자율 실행 루프

## 운영 모드

- 모드: `goal-handoff`
- 상태 기준: `docs/STATE.md`
- 실행 단위: 하나의 검증 가능한 목표
- 작업 흐름: 상태 확인 → 계획 → 구현 → 좁은 검증 → 전체 검증 → 상태 갱신 → 다음 목표

## 완료 체크포인트

잠재수요 v2 manifest를 기존 `ReplayScenario`에 선택적으로 연결했다. manifest가 없는 실행은
기존 지표·대여소 결과·trace 열을 보존하고, manifest 실행만 관측·합성·combined side-car와
출처별 trip-flow를 추가한다. 해시 불일치 fail-closed, 합성 실패 반납 억제, 모든 보존식 잔차
0을 소형 사례로 확인했으며 전체 42개 테스트가 통과했다. 정책 결과는 아직 생성하지 않았다.

## 다음 목표

동결된 50개 공통 seed `20251124..20251173`와 low/base/high에서 P0·P2-R paired 정책
민감도 실행기를 구현한다. 먼저 작은 seed smoke로 hash·요청합·보존식·출력 스키마를 검증한
뒤 실데이터 batch를 실행하고, 방향 전환 중단조건을 평가해 사후 민감도 범위로 보고한다.

## 자동 진행 범위

- 기존 데이터와 공개 읽기 API를 이용한 분석
- 소스·테스트·보고서·프로젝트 상태의 국소 변경
- Ruff, pytest, 프로젝트 검증 스크립트 실행
- 실패 원인이 분명한 경우 최대 2회의 국소 수정

## 중단 조건

- 새 인증키, 유료 서비스, 사용자의 외부 입력이 필요하다.
- 실제 운영정책·비용 가중치처럼 결과를 크게 바꾸는 제품 결정이 필요하다.
- 비밀정보, 인증, 결제, 배포, 데이터베이스 쓰기 또는 파괴적 작업을 건드린다.
- 하나의 체크포인트가 10개보다 많은 파일이나 여러 독립 하위시스템을 변경한다.
- 같은 검증 실패가 3회 반복되거나 로컬 검증이 불가능하다.

## 추론 등급

- `low`: 문서·상태·단순 요약
- `medium`: 작은 국소 수정과 명확한 테스트 보강
- `high`: 다중 파일 구현 또는 시뮬레이션 규칙 변경. 계획과 합격 기준을 먼저 기록한다.
- `very-high`: 제품·아키텍처·보안·비용 결정. 구현 전에 사용자에게 인계한다.

## 잠재수요 50-seed batch 합격 기준

- seed·시나리오별 P0와 P2-R이 같은 manifest hash와 요청 수를 사용한다.
- 모든 실행의 관측·합성·combined 요청합과 trip-flow·자전거 보존식 잔차가 0이다.
- low/base/high가 seed별 중첩되고 정확히 50개 공통 seed를 사용한다.
- 기존 관측 전용 홀드아웃 결과는 재계산해도 핵심 수치가 바뀌지 않는다.
- raw·요약 CSV, JSON, Markdown, 시각화가 같은 실행에서 재현된다.
- 방향 전환·25% 수요비중·backoff 5%·하드 상한 1% 중단조건을 자동 판정한다.
- `ruff check`, `ruff format --check`, `pytest`, `scripts/verify.ps1`이 통과한다.

## 현재 인계 상태

없음. 선택적 통합 소형 게이트가 통과해 50-seed batch 실행기를 구현할 수 있다.

## 사용량 경계

Codex가 계정의 남은 사용량 백분율을 직접 확인할 수 없으므로 정확한 5% 자동 정지는
보장하지 않는다. 목표 완료, 시스템 한도, 중단 조건 중 하나에 도달할 때까지 체크포인트를
연속 실행하고 사용자가 앱에서 사용량을 확인해 필요하면 목표를 일시중지한다.
