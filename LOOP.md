# 프로젝트 자율 실행 루프

## 운영 모드

- 모드: `goal-handoff`
- 상태 기준: `docs/STATE.md`
- 실행 단위: 하나의 검증 가능한 목표
- 작업 흐름: 상태 확인 → 계획 → 구현 → 좁은 검증 → 전체 검증 → 상태 갱신 → 다음 목표

## 완료 체크포인트

잠재수요 생성 계약 v1을 동결했다. 시간별 재고의 0대 경계와 학습기간 비품절 대여율로
low·base·high 중첩 요청열을 만들고, 경험적 상한·50개 공통 seed·관측/합성 ID·조건부 반납·
자전거 보존·누수 방지 규칙을 정의했다. 기대 합성량은 기존 관측 요청의 약 8.0~11.3%라
단일값을 고르지 않고 세 수준 전체를 사후 민감도로 보고한다.

## 다음 목표

`docs/LATENT_DEMAND_CONTRACT.md`만 구현하는 잠재수요 manifest 생성기와 소형 테스트를 만든다.
관측 전용 기존 실행은 바꾸지 않고, 입력 재정렬 불변·시나리오 중첩·고정 seed·상한·pool
backoff를 먼저 검증한다. 이 게이트 전에는 실데이터 정책 batch를 실행하지 않는다.

## 자동 진행 범위

- 기존 데이터와 공개 읽기 API를 이용한 분석
- 소스·테스트·보고서·프로젝트 상태의 국소 변경
- Ruff, pytest, 프로젝트 검증 스크립트 실행
- 실패 원인이 분명한 경우 최대 2회의 국소 수정

## 중단 조건

- 새 인증키, 유료 서비스, 사용자의 외부 입력이 필요하다.
- 실제 운영정책·비용 가중치처럼 결과를 크게 바꾸는 제품 결정이 필요하다.
- 비밀정보, 인증, 결제, 배포, 데이터베이스 쓰기 또는 파괴적 작업을 건드린다.
- 하나의 체크포인트가 10개보다 많은 파일이나 여러 독립 하위시스템을 변경한다.
- 같은 검증 실패가 3회 반복되거나 로컬 검증이 불가능하다.

## 추론 등급

- `low`: 문서·상태·단순 요약
- `medium`: 작은 국소 수정과 명확한 테스트 보강
- `high`: 다중 파일 구현 또는 시뮬레이션 규칙 변경. 계획과 합격 기준을 먼저 기록한다.
- `very-high`: 제품·아키텍처·보안·비용 결정. 구현 전에 사용자에게 인계한다.

## 잠재수요 생성기 합격 기준

- 비품절·결측·비60분 구간에는 합성 요청이 없다.
- `low ⊆ base ⊆ high`가 요청 ID로 증명된다.
- 같은 seed와 재정렬 입력은 동일 manifest, 다른 seed는 다른 확률 표본을 만든다.
- 관측·합성 ID namespace와 원본 관측행 불변을 검증한다.
- 강도·상한·OD backoff가 학습기간 밖 데이터를 읽지 않는다.
- 소형 테스트와 기존 관측 전용 30개 테스트가 함께 통과한다.

## 현재 인계 상태

없음. 세 수준 전체를 실행하므로 생성기 구현에 운영 가치선택은 필요 없다. 다만 시나리오별
정책 순위·개선 방향·최악 10% 결론이 바뀌면 정책 권고 전에 사용자에게 인계한다.

## 사용량 경계

Codex가 계정의 남은 사용량 백분율을 직접 확인할 수 없으므로 정확한 5% 자동 정지는
보장하지 않는다. 목표 완료, 시스템 한도, 중단 조건 중 하나에 도달할 때까지 체크포인트를
연속 실행하고 사용자가 앱에서 사용량을 확인해 필요하면 목표를 일시중지한다.
