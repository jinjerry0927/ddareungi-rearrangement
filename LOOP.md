# 프로젝트 자율 실행 루프

## 운영 모드

- 모드: `goal-handoff`
- 상태 기준: `docs/STATE.md`
- 실행 단위: 하나의 검증 가능한 목표
- 작업 흐름: 상태 확인 → 계획 → 구현 → 좁은 검증 → 전체 검증 → 상태 갱신 → 다음 목표

## 완료 체크포인트

기존 실행을 기본값으로 보존하면서 선택적 fleet 실행기를 구현했다. 배차 후 접근, 픽업 시점
reserve 7 재검사, 부분 픽업, busy 차량 미배차, 배송 완료 위치의 다음 접근 승계를 소형 반례로
검증했다. 기존 25개와 신규 4개를 합쳐 pytest 29개가 통과했고 실데이터는 아직 실행하지 않았다.

## 다음 목표

P0·기존 즉시출발 P2-R·fleet 1·2·3을 같은 165개 대여소·16,205개 요청에서 실행한다. 정책은
reserve 7로 고정하고 요청 전환·형평성·공차/적재 거리·차량 가동률을 CSV·JSON·Markdown·PNG로
보고한다. 같은 홀드아웃 재사용이므로 독립 검증이 아닌 사후 실행모델 민감도로 명시한다.

## 자동 진행 범위

- 기존 데이터와 공개 읽기 API를 이용한 분석
- 소스·테스트·보고서·프로젝트 상태의 국소 변경
- Ruff, pytest, 프로젝트 검증 스크립트 실행
- 실패 원인이 분명한 경우 최대 2회의 국소 수정

## 중단 조건

- 새 인증키, 유료 서비스, 사용자의 외부 입력이 필요하다.
- 실제 운영정책·비용 가중치처럼 결과를 크게 바꾸는 제품 결정이 필요하다.
- 비밀정보, 인증, 결제, 배포, 데이터베이스 쓰기 또는 파괴적 작업을 건드린다.
- 하나의 체크포인트가 10개보다 많은 파일이나 여러 독립 하위시스템을 변경한다.
- 같은 검증 실패가 3회 반복되거나 로컬 검증이 불가능하다.

## 추론 등급

- `low`: 문서·상태·단순 요약
- `medium`: 작은 국소 수정과 명확한 테스트 보강
- `high`: 다중 파일 구현 또는 시뮬레이션 규칙 변경. 계획과 합격 기준을 먼저 기록한다.
- `very-high`: 제품·아키텍처·보안·비용 결정. 구현 전에 사용자에게 인계한다.

## fleet 실데이터 민감도 합격 기준

- 모든 실행은 동일 165개 대여소·16,205개 요청·reserve 7을 사용한다.
- P0와 네 실행 시나리오의 요청 키·시각·대여소가 일치한다.
- 구제 - 악화 = P0 대비 순 실패 방지이며 보존식 잔차는 0이다.
- 접근 + 적재 = 총거리이고 차량 가동률은 0~1 범위다.
- fleet 1·2·3은 합성 범위이며 단일 실제 운영값으로 선택하지 않는다.
- 결과 CSV·JSON·Markdown·PNG가 재현 가능하게 생성된다.
- `scripts/verify.ps1`이 통과한다.

## 현재 인계 상태

없음. 소형 구현 게이트를 통과해 계약된 실데이터 사후 민감도를 실행할 수 있다.

## 사용량 경계

Codex가 계정의 남은 사용량 백분율을 직접 확인할 수 없으므로 정확한 5% 자동 정지는
보장하지 않는다. 목표 완료, 시스템 한도, 중단 조건 중 하나에 도달할 때까지 체크포인트를
연속 실행하고 사용자가 앱에서 사용량을 확인해 필요하면 목표를 일시중지한다.
