# 프로젝트 자율 실행 루프

## 운영 모드

- 모드: `goal-handoff`
- 상태 기준: `docs/STATE.md`
- 실행 단위: 하나의 검증 가능한 목표
- 작업 흐름: 상태 확인 → 계획 → 구현 → 좁은 검증 → 전체 검증 → 상태 갱신 → 다음 목표

## 완료 체크포인트

동결된 50개 seed × low/base/high × P0/P2-R 300개 실행을 완료했다. paired hash·요청 수
불일치 0건, 모든 trip-flow·자전거 보존식 잔차 0, 7개 사전 중단조건 PASS다. combined 실패
방지는 평균 1,509~1,532건으로 유지됐지만 잠재요청이 늘수록 관측요청 실패 방지는 934→882건,
악화 대여소는 평균 16.3→18.0곳으로 바뀌어 서비스 개선과 재고 경쟁을 함께 보고했다.

## 다음 목표

잠재수요 수준·seed별 대여소 실패 차이를 저장하고 반복 악화 지점을 찾는 공간 강건성 분석을
설계한다. 관측 기준 악화 12곳과 합성수요에서 새로 또는 반복해서 악화되는 지점을 분리하고,
악화 확률·실패 증감 분위수·세 수준 일관성을 표와 지도에 표시한다.

## 자동 진행 범위

- 기존 데이터와 공개 읽기 API를 이용한 분석
- 소스·테스트·보고서·프로젝트 상태의 국소 변경
- Ruff, pytest, 프로젝트 검증 스크립트 실행
- 실패 원인이 분명한 경우 최대 2회의 국소 수정

## 중단 조건

- 새 인증키, 유료 서비스, 사용자의 외부 입력이 필요하다.
- 실제 운영정책·비용 가중치처럼 결과를 크게 바꾸는 제품 결정이 필요하다.
- 비밀정보, 인증, 결제, 배포, 데이터베이스 쓰기 또는 파괴적 작업을 건드린다.
- 하나의 체크포인트가 10개보다 많은 파일이나 여러 독립 하위시스템을 변경한다.
- 같은 검증 실패가 3회 반복되거나 로컬 검증이 불가능하다.

## 추론 등급

- `low`: 문서·상태·단순 요약
- `medium`: 작은 국소 수정과 명확한 테스트 보강
- `high`: 다중 파일 구현 또는 시뮬레이션 규칙 변경. 계획과 합격 기준을 먼저 기록한다.
- `very-high`: 제품·아키텍처·보안·비용 결정. 구현 전에 사용자에게 인계한다.

## 잠재수요 대여소 공간 강건성 합격 기준

- 대여소별 P0·P2-R 요청 수와 manifest hash가 seed·수준별로 일치한다.
- 대여소 실패 차이 합이 정책 전체 실패 방지와 정확히 재조정된다.
- 관측·합성·combined를 섞지 않고 별도 공간 안정성 지표로 저장한다.
- 50개 seed 악화 확률, 실패 증감 중앙값·p10·p90을 수준별로 산출한다.
- 관측 기준 악화·잠재수요 반복 악화·새 악화 대여소를 명시적으로 구분한다.
- 좌표 165개가 모두 1:1 매핑되고 지도 렌더링을 시각 확인한다.
- `ruff check`, `ruff format --check`, `pytest`, `scripts/verify.ps1`이 통과한다.

## 현재 인계 상태

없음. 50-seed batch가 중단조건 없이 끝나 공간 강건성 분석을 진행할 수 있다.

## 사용량 경계

Codex가 계정의 남은 사용량 백분율을 직접 확인할 수 없으므로 정확한 5% 자동 정지는
보장하지 않는다. 목표 완료, 시스템 한도, 중단 조건 중 하나에 도달할 때까지 체크포인트를
연속 실행하고 사용자가 앱에서 사용량을 확인해 필요하면 목표를 일시중지한다.
