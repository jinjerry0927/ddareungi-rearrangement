# 프로젝트 자율 실행 루프

## 운영 모드

- 모드: `goal-handoff`
- 상태 기준: `docs/STATE.md`
- 실행 단위: 하나의 검증 가능한 목표
- 작업 흐름: 상태 확인 → 계획 → 구현 → 좁은 검증 → 전체 검증 → 상태 갱신 → 다음 목표

## 완료 체크포인트

잠재수요 v1 manifest 생성기와 전용 테스트 8개를 구현했다. 165개 대여소 메모리 smoke에서
low·base·high 1,303·1,591·1,878건과 최종 backoff 0.56%를 확인했다. 그러나 p95 하드 상한이
77/2,142구간(3.59%)에서 작동해 1% 품질 게이트를 넘고 후보 101건을 제거했다. 기존
시뮬레이터에는 연결하지 않았고 정책 batch도 실행하지 않았다.

## 다음 목표

HOLD. p95는 학습 강도의 경험적 경계로 유지하되 실현건수 하드 상한을
`max(학습 p95, Poisson 99백분위수)`로 분리하는 권장안을 사용자가 확인하면 계약 버전을 올리고
상한 구현·테스트·165개 manifest 게이트를 다시 실행한다. 확인 전에는 통합하지 않는다.

## 자동 진행 범위

- 기존 데이터와 공개 읽기 API를 이용한 분석
- 소스·테스트·보고서·프로젝트 상태의 국소 변경
- Ruff, pytest, 프로젝트 검증 스크립트 실행
- 실패 원인이 분명한 경우 최대 2회의 국소 수정

## 중단 조건

- 새 인증키, 유료 서비스, 사용자의 외부 입력이 필요하다.
- 실제 운영정책·비용 가중치처럼 결과를 크게 바꾸는 제품 결정이 필요하다.
- 비밀정보, 인증, 결제, 배포, 데이터베이스 쓰기 또는 파괴적 작업을 건드린다.
- 하나의 체크포인트가 10개보다 많은 파일이나 여러 독립 하위시스템을 변경한다.
- 같은 검증 실패가 3회 반복되거나 로컬 검증이 불가능하다.

## 추론 등급

- `low`: 문서·상태·단순 요약
- `medium`: 작은 국소 수정과 명확한 테스트 보강
- `high`: 다중 파일 구현 또는 시뮬레이션 규칙 변경. 계획과 합격 기준을 먼저 기록한다.
- `very-high`: 제품·아키텍처·보안·비용 결정. 구현 전에 사용자에게 인계한다.

## 잠재수요 상한 재설계 합격 기준

- 계약 버전과 문서·코드·테스트의 상한 정의가 일치한다.
- p95 강도 경계와 Poisson 실현건수 안전 상한의 역할이 분리된다.
- 기존 중첩·재현·입력순서·누수 테스트를 모두 보존한다.
- 165개 manifest에서 최종 backoff 5% 이하, 상한 도달 1% 미만을 통과한다.
- 기존 관측 전용 30개와 잠재수요 8개 테스트가 함께 통과한다.

## 현재 인계 상태

사용자 확인 필요. 현재 p95 하드 상한을 유지하면 3.59% 구간과 요청 101건이 잘려 결과가 하향
편향될 수 있다. 권장안은 p95를 강도 경계로 유지하고 하드 상한만 Poisson 99백분위수 이상으로
분리하는 것이다.

## 사용량 경계

Codex가 계정의 남은 사용량 백분율을 직접 확인할 수 없으므로 정확한 5% 자동 정지는
보장하지 않는다. 목표 완료, 시스템 한도, 중단 조건 중 하나에 도달할 때까지 체크포인트를
연속 실행하고 사용자가 앱에서 사용량을 확인해 필요하면 목표를 일시중지한다.
