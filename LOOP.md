# 프로젝트 자율 실행 루프

## 운영 모드

- 모드: `goal-handoff`
- 상태 기준: `docs/STATE.md`
- 실행 단위: 하나의 검증 가능한 목표
- 작업 흐름: 상태 확인 → 계획 → 구현 → 좁은 검증 → 전체 검증 → 상태 갱신 → 다음 목표

## 완료 체크포인트

대여소 수준 원자료 74,250행과 요약 1,485행을 생성하고 450개 seed-수준-출처 그룹의 실패
차이 합을 전체 지표와 잔차 0으로 재조정했다. 세 수준 모두 persistent 악화는 8곳이고 7곳은
모든 수준에서 반복됐다. high에서는 `3640`이 50/50 seed 악화·평균 실패 10.46건 증가,
관측 기준 비악화였던 `2387`이 새 persistent로 확인됐다. 165개 좌표 지도 QA도 통과했다.

## 다음 목표

persistent 7곳과 high 신규 `2387`의 운영 경로를 진단한다. 정책을 바꾸지 않고 재배치 유출,
빈 재고 시간, 관측·합성 요청 구성, P0 대비 실패 차이를 전체 개선 대여소와 비교해 반복 악화가
donor 역할과 연결되는지 검증한다.

## 자동 진행 범위

- 기존 데이터와 공개 읽기 API를 이용한 분석
- 소스·테스트·보고서·프로젝트 상태의 국소 변경
- Ruff, pytest, 프로젝트 검증 스크립트 실행
- 실패 원인이 분명한 경우 최대 2회의 국소 수정

## 중단 조건

- 새 인증키, 유료 서비스, 사용자의 외부 입력이 필요하다.
- 실제 운영정책·비용 가중치처럼 결과를 크게 바꾸는 제품 결정이 필요하다.
- 비밀정보, 인증, 결제, 배포, 데이터베이스 쓰기 또는 파괴적 작업을 건드린다.
- 하나의 체크포인트가 10개보다 많은 파일이나 여러 독립 하위시스템을 변경한다.
- 같은 검증 실패가 3회 반복되거나 로컬 검증이 불가능하다.

## 추론 등급

- `low`: 문서·상태·단순 요약
- `medium`: 작은 국소 수정과 명확한 테스트 보강
- `high`: 다중 파일 구현 또는 시뮬레이션 규칙 변경. 계획과 합격 기준을 먼저 기록한다.
- `very-high`: 제품·아키텍처·보안·비용 결정. 구현 전에 사용자에게 인계한다.

## persistent 악화 대여소 원인 진단 합격 기준

- persistent·새 persistent ID 집합을 현재 공간 요약과 정확히 일치시킨다.
- seed별 P2-R 재배치 유출·빈 시간·요청 출처를 대여소 실패 차이와 같은 키로 연결한다.
- 대여소 운영 지표 합이 정책 전체 이동·빈 시간과 재조정된다.
- persistent 집단과 나머지 대여소의 분포를 가중치 없는 기술통계로 비교한다.
- 상관관계를 즉시 인과로 표현하지 않고 대표 trace로 시간 선후를 별도 확인한다.
- 정책 파라미터 변경이나 대여소별 예외 규칙은 이 진단 Goal 범위에서 만들지 않는다.
- `ruff check`, `ruff format --check`, `pytest`, `scripts/verify.ps1`이 통과한다.

## 현재 인계 상태

없음. 반복 악화 지점이 식별돼 읽기 전용 원인 진단을 진행할 수 있다.

## 사용량 경계

Codex가 계정의 남은 사용량 백분율을 직접 확인할 수 없으므로 정확한 5% 자동 정지는
보장하지 않는다. 목표 완료, 시스템 한도, 중단 조건 중 하나에 도달할 때까지 체크포인트를
연속 실행하고 사용자가 앱에서 사용량을 확인해 필요하면 목표를 일시중지한다.
