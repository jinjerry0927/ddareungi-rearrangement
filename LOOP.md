# 프로젝트 자율 실행 루프

## 운영 모드

- 모드: `goal-handoff`
- 상태 기준: `docs/STATE.md`
- 실행 단위: 하나의 검증 가능한 목표
- 작업 흐름: 상태 확인 → 계획 → 구현 → 좁은 검증 → 전체 검증 → 상태 갱신 → 다음 목표

## 완료 체크포인트

README를 오래된 M2 진행 설명에서 M9 포트폴리오 요약으로 개편했다. 16,205건 홀드아웃의
P0·P2·P3 수치, reserve 7의 서비스-보호 절충, 실험 파이프라인, 12개 재현 명령, 10개
분석 보고서와 해석 한계를 첫 화면에 연결했다. 로컬 링크 18개와 정책 결과 3행을 검증했다.

## 다음 목표

현재 P3의 공급지 즉시 출발 가정을 코드와 이벤트 순서에서 감사하고, 첫 접근 이동과 연속
차량 경로를 추가할 수 있는 최소 운영 계약을 설계한다. 차량 수·depot·복귀 정책처럼 결과를
크게 바꾸는 값은 데이터 근거와 선택지를 문서화하며, 계약이 결정되기 전에는 구현하지 않는다.

## 자동 진행 범위

- 기존 데이터와 공개 읽기 API를 이용한 분석
- 소스·테스트·보고서·프로젝트 상태의 국소 변경
- Ruff, pytest, 프로젝트 검증 스크립트 실행
- 실패 원인이 분명한 경우 최대 2회의 국소 수정

## 중단 조건

- 새 인증키, 유료 서비스, 사용자의 외부 입력이 필요하다.
- 실제 운영정책·비용 가중치처럼 결과를 크게 바꾸는 제품 결정이 필요하다.
- 비밀정보, 인증, 결제, 배포, 데이터베이스 쓰기 또는 파괴적 작업을 건드린다.
- 하나의 체크포인트가 10개보다 많은 파일이나 여러 독립 하위시스템을 변경한다.
- 같은 검증 실패가 3회 반복되거나 로컬 검증이 불가능하다.

## 추론 등급

- `low`: 문서·상태·단순 요약
- `medium`: 작은 국소 수정과 명확한 테스트 보강
- `high`: 다중 파일 구현 또는 시뮬레이션 규칙 변경. 계획과 합격 기준을 먼저 기록한다.
- `very-high`: 제품·아키텍처·보안·비용 결정. 구현 전에 사용자에게 인계한다.

## 차량 경로 계약 설계 합격 기준

- 기존 직접 운송의 출발·도착·동시성 가정을 코드 근거로 설명한다.
- 첫 접근·작업 간 이동·대기·복귀 중 최소 반영 범위를 구분한다.
- 차량 수·초기 위치·depot에 공식 근거가 없으면 합성 가정으로 명시한다.
- P3 정책 로직 변경과 운영 모델 민감도를 섞지 않는 평가 계약을 제시한다.
- 구현 전 필요한 제품 결정과 자동 진행 가능한 범위를 분리한다.

## 현재 인계 상태

없음. 다음 단계는 읽기 전용 코드 감사와 설계 문서 작성이며 시뮬레이터 동작을 바꾸지 않는다.

## 사용량 경계

Codex가 계정의 남은 사용량 백분율을 직접 확인할 수 없으므로 정확한 5% 자동 정지는
보장하지 않는다. 목표 완료, 시스템 한도, 중단 조건 중 하나에 도달할 때까지 체크포인트를
연속 실행하고 사용자가 앱에서 사용량을 확인해 필요하면 목표를 일시중지한다.
