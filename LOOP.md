# 프로젝트 자율 실행 루프

## 운영 모드

- 모드: `goal-handoff`
- 상태 기준: `docs/STATE.md`
- 실행 단위: 하나의 검증 가능한 목표
- 작업 흐름: 상태 확인 → 계획 → 구현 → 좁은 검증 → 전체 검증 → 상태 갱신 → 다음 목표

## 완료 체크포인트

승인된 잠재수요 v2 상한을 구현했다. p95를 시간당 강도 경계로 적용하고 실현건수 하드 상한을
Poisson q99 이상으로 분리했다. 165개 메모리 smoke에서 low·base·high 1,382·1,673·1,964건,
최종 backoff 0.56%, 하드 상한 도달 0.61%로 두 품질 게이트를 통과했다. 정책 결과는 아직
생성하지 않았다.

## 다음 목표

잠재수요 manifest를 기존 `ReplayScenario`에 선택적으로 연결한다. 관측 전용 기본 동작과 핵심
홀드아웃 수치를 그대로 보존하고, 관측·합성·combined 지표, 합성 실패 반납 억제, 출처별·전체
자전거 보존식을 소형 사례로 먼저 검증한다. 이 게이트 전에는 50개 seed 정책 batch를 실행하지
않는다.

## 자동 진행 범위

- 기존 데이터와 공개 읽기 API를 이용한 분석
- 소스·테스트·보고서·프로젝트 상태의 국소 변경
- Ruff, pytest, 프로젝트 검증 스크립트 실행
- 실패 원인이 분명한 경우 최대 2회의 국소 수정

## 중단 조건

- 새 인증키, 유료 서비스, 사용자의 외부 입력이 필요하다.
- 실제 운영정책·비용 가중치처럼 결과를 크게 바꾸는 제품 결정이 필요하다.
- 비밀정보, 인증, 결제, 배포, 데이터베이스 쓰기 또는 파괴적 작업을 건드린다.
- 하나의 체크포인트가 10개보다 많은 파일이나 여러 독립 하위시스템을 변경한다.
- 같은 검증 실패가 3회 반복되거나 로컬 검증이 불가능하다.

## 추론 등급

- `low`: 문서·상태·단순 요약
- `medium`: 작은 국소 수정과 명확한 테스트 보강
- `high`: 다중 파일 구현 또는 시뮬레이션 규칙 변경. 계획과 합격 기준을 먼저 기록한다.
- `very-high`: 제품·아키텍처·보안·비용 결정. 구현 전에 사용자에게 인계한다.

## 잠재수요 시뮬레이터 통합 합격 기준

- manifest가 없으면 기존 관측 전용 결과와 trace가 바이트 수준으로 같다.
- manifest가 있으면 정책별 동일 scenario hash를 fail-closed 확인한다.
- 관측·합성·combined 요청/성공/실패 합계가 각각 일치한다.
- 합성 실패는 반납을 만들지 않고 성공한 내부 합성 여행만 조건부 반납한다.
- 관측·합성 운행 중 수량을 분리하면서 전체 자전거 보존식 잔차가 0이다.
- 기존 관측 30개와 잠재수요 9개 테스트를 모두 보존한다.

## 현재 인계 상태

없음. v2 상한과 manifest 품질 게이트가 통과해 선택적 시뮬레이터 통합을 진행할 수 있다.

## 사용량 경계

Codex가 계정의 남은 사용량 백분율을 직접 확인할 수 없으므로 정확한 5% 자동 정지는
보장하지 않는다. 목표 완료, 시스템 한도, 중단 조건 중 하나에 도달할 때까지 체크포인트를
연속 실행하고 사용자가 앱에서 사용량을 확인해 필요하면 목표를 일시중지한다.
