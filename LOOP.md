# 프로젝트 자율 실행 루프

## 운영 모드

- 모드: `goal-handoff`
- 상태 기준: `docs/STATE.md`
- 실행 단위: 하나의 검증 가능한 목표
- 작업 흐름: 상태 확인 → 계획 → 구현 → 좁은 검증 → 전체 검증 → 상태 갱신 → 다음 목표

## 완료 체크포인트

현재 transfer 3회가 차량 3대가 아니라 독립 동시 작업 상한이고, 차량→공급지 접근과 연속
위치가 없음을 코드에서 확인했다. 정책과 영속 fleet 실행기를 분리하고 fleet 1·2·3, 합성
greedy medoids, 픽업 시점 재고 재확인, 공차·적재 거리 분해를 구현 전 계약으로 동결했다.

## 다음 목표

`docs/VEHICLE_ROUTING_CONTRACT.md`의 계약대로 기존 실행을 보존하는 fleet 실행기를 구현한다.
소형 접근·픽업·배송·busy 테스트와 결정론적 greedy medoids를 먼저 통과시키고, 실패하면 실제
5일 데이터를 실행하지 않는다.

## 자동 진행 범위

- 기존 데이터와 공개 읽기 API를 이용한 분석
- 소스·테스트·보고서·프로젝트 상태의 국소 변경
- Ruff, pytest, 프로젝트 검증 스크립트 실행
- 실패 원인이 분명한 경우 최대 2회의 국소 수정

## 중단 조건

- 새 인증키, 유료 서비스, 사용자의 외부 입력이 필요하다.
- 실제 운영정책·비용 가중치처럼 결과를 크게 바꾸는 제품 결정이 필요하다.
- 비밀정보, 인증, 결제, 배포, 데이터베이스 쓰기 또는 파괴적 작업을 건드린다.
- 하나의 체크포인트가 10개보다 많은 파일이나 여러 독립 하위시스템을 변경한다.
- 같은 검증 실패가 3회 반복되거나 로컬 검증이 불가능하다.

## 추론 등급

- `low`: 문서·상태·단순 요약
- `medium`: 작은 국소 수정과 명확한 테스트 보강
- `high`: 다중 파일 구현 또는 시뮬레이션 규칙 변경. 계획과 합격 기준을 먼저 기록한다.
- `very-high`: 제품·아키텍처·보안·비용 결정. 구현 전에 사용자에게 인계한다.

## fleet 실행기 합격 기준

- 기존 `simulate_replay` 결과와 25개 테스트를 그대로 보존한다.
- 차량 배정, 초기 위치, 이벤트 결과가 결정론적이다.
- 같은 차량의 busy 구간이 겹치지 않고 픽업 전에는 공급지 재고를 차감하지 않는다.
- 픽업 후 donor reserve 7과 전체 자전거 보존식이 유지된다.
- 접근·적재·총거리와 차량 busy 시간의 분해 합이 일치한다.
- 소형 테스트 통과 전에는 5일 실데이터를 실행하지 않는다.
- 새 dependency와 완전 VRP를 추가하지 않는다.

## 현재 인계 상태

없음. fleet 1·2·3은 실제 운영값이 아닌 합성 민감도 범위로 동결해 구현할 수 있다.

## 사용량 경계

Codex가 계정의 남은 사용량 백분율을 직접 확인할 수 없으므로 정확한 5% 자동 정지는
보장하지 않는다. 목표 완료, 시스템 한도, 중단 조건 중 하나에 도달할 때까지 체크포인트를
연속 실행하고 사용자가 앱에서 사용량을 확인해 필요하면 목표를 일시중지한다.
