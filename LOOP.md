# 프로젝트 자율 실행 루프

## 운영 모드

- 모드: `goal-handoff`
- 상태 기준: `docs/STATE.md`
- 실행 단위: 하나의 검증 가능한 목표
- 작업 흐름: 상태 확인 → 계획 → 구현 → 좁은 검증 → 전체 검증 → 상태 갱신 → 다음 목표

## 완료 체크포인트

동일 165개·16,205건에서 P0·즉시출발 P2-R·fleet 1·2·3을 실행했다. fleet 3은 P0보다
1,154건을 방지했지만 즉시출발보다 실패가 60건 많았고, 공차 1,127.3km 때문에 총거리가
4.11배였다. fleet 1·2·3은 합성 범위로만 보고하고 단일 운영값을 선택하지 않았다.

## 다음 목표

품절 때문에 공개 대여이력에 남지 않은 잠재수요의 생성 계약을 설계한다. 관측·합성 요청을
완전히 분리하고, 낮음·기준·높음 시나리오의 근거·상한·고정 시드·검증 누수 방지 규칙을 먼저
문서화한다. 가정 선택이 결론을 크게 바꾸면 구현 전에 사용자에게 인계한다.

## 자동 진행 범위

- 기존 데이터와 공개 읽기 API를 이용한 분석
- 소스·테스트·보고서·프로젝트 상태의 국소 변경
- Ruff, pytest, 프로젝트 검증 스크립트 실행
- 실패 원인이 분명한 경우 최대 2회의 국소 수정

## 중단 조건

- 새 인증키, 유료 서비스, 사용자의 외부 입력이 필요하다.
- 실제 운영정책·비용 가중치처럼 결과를 크게 바꾸는 제품 결정이 필요하다.
- 비밀정보, 인증, 결제, 배포, 데이터베이스 쓰기 또는 파괴적 작업을 건드린다.
- 하나의 체크포인트가 10개보다 많은 파일이나 여러 독립 하위시스템을 변경한다.
- 같은 검증 실패가 3회 반복되거나 로컬 검증이 불가능하다.

## 추론 등급

- `low`: 문서·상태·단순 요약
- `medium`: 작은 국소 수정과 명확한 테스트 보강
- `high`: 다중 파일 구현 또는 시뮬레이션 규칙 변경. 계획과 합격 기준을 먼저 기록한다.
- `very-high`: 제품·아키텍처·보안·비용 결정. 구현 전에 사용자에게 인계한다.

## 잠재수요 계약 설계 합격 기준

- 관측 요청과 합성 요청의 ID·출처·지표를 분리한다.
- 품절 구간만 합성하고 비품절 관측 요청으로 강도를 추정한다.
- 학습기간에서만 생성 규칙을 정하고 홀드아웃 결과로 재튜닝하지 않는다.
- 낮음·기준·높음 시나리오와 상한을 임의 단일 배수 대신 근거로 제시한다.
- 같은 시드에서 완전히 재현되고 정책별 동일 합성 요청을 사용한다.
- 잠재수요가 반납을 생성하는 계약과 자전거 보존식을 함께 정의한다.

## 현재 인계 상태

없음. 다음 단계는 읽기 전용 방법 설계이며 합성 요청을 아직 생성하지 않는다.

## 사용량 경계

Codex가 계정의 남은 사용량 백분율을 직접 확인할 수 없으므로 정확한 5% 자동 정지는
보장하지 않는다. 목표 완료, 시스템 한도, 중단 조건 중 하나에 도달할 때까지 체크포인트를
연속 실행하고 사용자가 앱에서 사용량을 확인해 필요하면 목표를 일시중지한다.
